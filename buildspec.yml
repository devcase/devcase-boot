version: 0.2

#env:
#  variables:
#    key: "value"
#    key: "value"
#  parameter-store:
#    key: "value"
#    key: "value"
            
phases:
#  install:
#    commands:
#      - command
#      - command
#  pre_build:
#    commands:
#      - command
#      - command
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
#  post_build:
#    commands:
#      - command
#      - command
artifacts:
  files:
    - devcase-boot-samples/devcase-boot-sample-dwftaglibs/target/devcase-boot-sample-dwftaglibs-*.war
  discard-paths: yes
#  base-directory: location